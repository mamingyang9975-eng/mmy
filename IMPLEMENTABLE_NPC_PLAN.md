# 可直接加入游戏的员工与关卡策划案

## 目标

这份策划案只解决两件事：

1. 在不推翻现有原型结构的前提下，把“员工”正式加入游戏。
2. 让新增内容能直接进入开发，而不是停留在世界观层面。

当前项目已经具备这些基础：

- 房间驱动结构：`rooms.ts`
- 身份解释系统：`intruder / guidedVisitor / maintenanceCandidate`
- 住户移动与响应：`residentStates`
- 巡逻与护送：扫描机、护送机、门禁、终端

因此这次扩展不建议新起一整套 NPC 系统，而建议在现有 `resident` 机制上扩一层“员工行为”。

---

## 一期开发目标

一期只做一个完整闭环：

- 新增 1 个员工类型：前台员工
- 新增 1 个房间：接待缓冲区
- 新增 1 条玩法规则：员工的行为会给玩家身份解释加权
- 新增 1 条叙事线索：设施中的人工确认也只是流程的一部分

这套闭环做完后，玩家会明确感受到：

- 地图扩大了
- 有“人”了
- 这些人和世界观是连着的
- 他们不是装饰，而是玩法的一部分

---

## 一期新增内容

## 房间结构

建议把当前流程调整为：

1. 前奏区
2. 房间 1：外门登记
3. 房间 2：接待缓冲区（新增）
4. 房间 3：住户服务点
5. 房间 4：维修代行区
6. 房间 5：广播复核区

也就是说，把现有 `room-2`、`room-3`、`room-4` 顺次后移，在登记之后插入一个新房间。

这样好处很明显：

- 现有房间逻辑不用删
- 新房间能承担“从纯系统交互过渡到有人参与”的任务
- 玩家会更自然地接受后续住户、服务、广播这些区域

---

## 新房间策划：接待缓冲区

## 房间定位

这是登记完成后进入内部设施前的一段半公开区域。

它的作用不是再教玩家一次登记，而是让玩家第一次理解：

“即使是人类员工，也是在帮系统完成对你的归类。”

## 房间目标

玩家需要借助前台员工的接待动作，让自己通过内门。

直接冲门会失败。
只登记还不够。
必须让系统产生一次“这个人刚被接待过”的解释。

## 空间元素

- 一个入口门
- 一个内部接待门
- 一个接待台
- 一个候位区
- 一个前台终端
- 一个巡逻扫描机
- 一个前台员工

## 玩法流程

建议流程如下：

1. 玩家从房间左侧进入。
2. 右侧有内部门，但默认不会因为“已登记访客”直接开启。
3. 中间有候位区，玩家需要站入其中并短暂停留。
4. 前台员工会周期性走向前台终端，执行“确认接待”动作。
5. 若员工确认时，玩家位于候位区内，则玩家获得短时间的“已接待访客”状态。
6. 在状态持续期间，内部接待门允许放行。

## 玩家学到的东西

- 人类员工不会主动帮你，但他们的工作流程可以被你利用。
- 员工不是故事背景板，而是“解释系统”的人工接口。
- 世界不只是机器误判你，连人工确认也只是在巩固误判。

---

## 前台员工设计

## 角色定位

前台员工是第一类正式加入玩法的人类岗位。

他们的特点是：

- 低威胁
- 不追逐
- 不告警
- 不直接给剧情对话
- 会通过岗位动作改变系统对玩家的理解

## 视觉定位

建议和住户明显区分：

- 更规整的站姿
- 固定工牌色
- 行走节奏更稳定
- 靠近终端时有短暂停顿

## 行为状态

前台员工一期只需要 4 个状态：

- `idleAtDesk`
- `walkingToTerminal`
- `checkingQueue`
- `returningToDesk`

## 状态循环

建议固定循环：

1. 在桌前停留 2 到 4 秒
2. 走向前台终端
3. 在终端前确认 1.5 秒
4. 检查候位区 1 秒
5. 返回桌前

只要玩家在 `checkingQueue` 状态时位于候位区内，就触发一次“接待确认”。

---

## 新机制：员工影响解释

## 一期规则

不新增复杂身份标签，只新增一个短时上下文状态：

- `receptionConfirmedMs`

效果：

- 倒计时期间，提高 `guidedVisitor`
- 倒计时期间，接待门允许把玩家视为合法访客

建议数值：

- 持续时间：5000ms
- 加权方式：
  - `guidedVisitor +4`
  - `intruder -2`

## 触发条件

满足以下条件时触发：

- 玩家位于候位区
- 前台员工处于 `checkingQueue`
- 玩家没有奔跑

## 失败反馈

若玩家在员工检查时：

- 不在候位区
- 高速移动
- 贴近内门乱晃

则不触发接待确认，扫描机会继续按原规则观察。

---

## 房间文案建议

## 房间标题

接待 / 缓冲确认

## 提示文案

“登记只说明你来过。要进去，还得让这里有人把你接进流程里。”

## 墙面标语

- “请在候位区等待人工确认”
- “低置信访客需补充接待记录”
- “确认完成前请勿接近内门”

## 通关提示文案

“前台没有认识你，她只是完成了一次确认。系统已经把这当成你应当出现在这里的证据。”

---

## 二期扩展内容

当一期跑通后，再加第二类员工。

## 后勤员工

## 房间适配

适合加入当前住户服务点房间。

## 行为逻辑

- 在补给点、服务托盘、侧门之间来回移动
- 推着服务车
- 周期性停下整理货物

## 玩法作用

- 玩家跟在后勤员工附近移动时，`maintenanceCandidate` 轻微上升
- 扫描机更倾向把玩家理解成“内部流转相关人员”

## 叙事作用

- 让设施更像真的在运行
- 让“内部流转”成为有人的流程，而不只是机器门禁

---

## 三期扩展内容

## 档案员

适合加入广播复核区。

## 行为逻辑

- 在复核台、观察窗、广播台之间移动
- 某些时刻会停在观察位
- 会周期性“更新记录”

## 玩法作用

- 玩家在正确位置等待时，可短暂降低 `intruder`
- 某些出口只有在“已记录”窗口期间才能稳定通过

## 叙事作用

- 直接把“解释就是记录”这件事做出来

---

## AI 控制是否可行

结论：

可行，但不建议一开始上“通用 AI”。

更适合这个项目的方案是：

- 底层用规则驱动状态机
- 中层用简单评分选择行为
- 只在文案和细节上表现出“像 AI 在工作”

不建议上来做大模型式 AI 或复杂行为树，原因有三个：

1. 你的游戏核心不是和 NPC 对话，而是利用他们的岗位流程。
2. 关卡需要强可读性，如果 NPC 行为太自由，玩家会看不懂规则。
3. 现有代码已经是高确定性的状态系统，继续沿这个方向最稳。

---

## 推荐的员工 AI 方案

推荐采用三层结构。

## 第一层：岗位日程

决定员工“此刻应该去哪里”。

例如前台员工：

- 桌前
- 终端
- 候位区观察位
- 返回桌前

这层本质上是时间片轮换或巡逻点循环。

## 第二层：情境反应

决定员工是否因为玩家或系统状态而短暂改变行为。

例如：

- 玩家在候位区安静等待：进入 `checkingQueue`
- 警报触发：员工停在桌前不再巡回
- 门已开：员工提前返回岗位

这层只做小范围插入，不推翻岗位循环。

## 第三层：解释影响

员工不是直接抓玩家，而是修改系统如何看玩家。

例如：

- 接待确认时，增加 `guidedVisitor`
- 后勤同行时，增加 `maintenanceCandidate`
- 档案复核时，降低 `intruder`

这一层最重要，因为它直接服务主题。

---

## 为什么这个 AI 方案适合你现在的项目

因为它可以直接贴合现有代码结构：

- `RoomDefinition` 已经适合挂静态角色配置
- `residentStates` 已经有位置和模式
- `session.updateIntent()` 已经是集中处理解释状态的地方
- `GameScene` 已经有渲染、移动、门禁、提示刷新

所以最自然的做法是：

1. 先把 `resident` 泛化为 `actor`
2. 给 `actor` 增加 `role` 和 `state`
3. 在 `session` 中更新员工状态与影响
4. 在 `GameScene` 中复用现有人形渲染方式

---

## 技术落地建议

## 数据结构建议

建议新增类型：

- `StaffRole`
- `StaffDefinition`
- `StaffRuntime`

建议字段：

- `role`
- `position`
- `patrolPoints`
- `workPoints`
- `state`
- `stateTimerMs`
- `influenceRadius`
- `influenceType`

## 最小状态集合

前台员工先只需要：

- `idle`
- `moving`
- `working`
- `checking`

## 运行逻辑建议

每帧更新流程：

1. 更新员工当前状态计时器
2. 若状态结束，切换到下一岗位节点
3. 若玩家在特定区域且满足条件，触发员工影响
4. 把影响写回当前房间运行时

---

## 对“AI 控制人物行动”的具体判断

如果你的意思是：

### 1. 让不同岗位的人有不同自动行为

这个非常可行，而且应该做。

这会明显提升：

- 世界真实感
- 剧情感
- 关卡变化
- 可扩展性

### 2. 让人物像开放世界 NPC 那样自由决策

不建议现在做。

会带来的问题：

- 可读性下降
- 关卡解法不稳定
- 调试成本暴涨
- 玩家无法判断自己失败是因为规则还是随机性

### 3. 让人物由生成式 AI 即时驱动

目前不适合这个项目阶段。

原因：

- 成本高
- 结果不稳定
- 很难保证可重复性
- 你的项目核心收益并不在这里

更合适的做法是：

“看起来像有岗位智能，但本质是精心设计的状态机。”

这对潜行和解谜游戏反而是优点。

---

## 开发顺序建议

建议按这个顺序推进：

1. 插入新房间“接待缓冲区”
2. 做前台员工状态机
3. 做 `receptionConfirmedMs` 机制
4. 让接待门吃这个状态
5. 补文案和 HUD 提示
6. 测试玩懂率

如果这一套成立，再继续：

7. 给服务层加后勤员工
8. 给复核层加档案员
9. 再考虑办公层和最终救出目标

---

## 验收标准

这版内容是否成功，可以看三个标准：

1. 玩家能否不靠长文本就理解“前台员工在帮助系统确认我”。
2. 玩家是否感觉新房间不是单纯多了一扇门，而是多了一种社会流程。
3. 玩家是否开始期待后面还会有别的岗位人物影响自己身份。

只要这三点成立，这次扩展就是成功的。
